worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;

    upstream app {
        server localhost:3000;  # If your app runs a backend on a different port
    }

    server {
        listen 80;
        server_name localhost;

        location / {
            C:\Users\ASHISH PAWAR\OneDrive\Desktop\Portfolio web\index.html;
        }

        # Uncomment below if you have a backend API running on a different port
        # location /api/ {
        #     proxy_pass http://app;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }
    }
}
